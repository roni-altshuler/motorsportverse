# F1 Predictions - Development Dependencies
# Install with: pip install -r requirements-dev.txt
# (Includes runtime deps automatically via -r requirements.txt.)

-r requirements.txt

# Tests
pytest~=8.3.4
pytest-cov~=6.0.0

# Static analysis
ruff~=0.8.4
mypy~=1.13.0

# Schema validation (pydantic models that mirror the website TS types)
pydantic~=2.10.4

# ML rigor (Tier 1-2 work) — staged for future tracks, not yet imported.
# mapie 0.9.2 transitively pins scikit-learn<1.6 which conflicts with the
# runtime scikit-learn~=1.8.0 pin in requirements.txt, breaking CI install.
# Re-enable per-package when the consuming feature lands and pick a
# scikit-learn-1.8-compatible version.
#   - optuna  → re-enable for A-P2.x hyperparameter search
#   - mlflow  → re-enable when registry-backed experiment tracking lands
#   - mapie   → re-enable for A-P2.3 conformal prediction intervals
# optuna~=4.1.0
# mlflow~=2.19.0
# mapie~=0.9.2

# Betting / backtest (Tier 1 work)
duckdb~=1.1.3          # single-file DB for odds snapshots + backtest runs
requests~=2.32.3       # odds API clients
python-dotenv~=1.0.1   # loads .env for ODDS_API_KEY and friends
