import HomePage from "@/components/HomePage";

// Server component: HomePage reads the build-time FE dataset via getNascarData()
// and renders the ported home + marketing sections.
export default function Page() {
  return <HomePage />;
}
