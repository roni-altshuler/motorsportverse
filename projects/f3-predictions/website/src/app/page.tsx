import HomePage from "@/components/HomePage";

// Server component: HomePage reads the build-time F3 dataset via getF3Data()
// and renders the ported home + marketing sections.
export default function Page() {
  return <HomePage />;
}
