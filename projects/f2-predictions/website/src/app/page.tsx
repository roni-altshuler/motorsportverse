import HomePage from "@/components/HomePage";

// Server component: HomePage reads the build-time F2 dataset via getF2Data()
// and renders the ported home + marketing sections.
export default function Page() {
  return <HomePage />;
}
